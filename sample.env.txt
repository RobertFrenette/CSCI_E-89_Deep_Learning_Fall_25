# ============================================================================
# IMPORTANT: Make a copy of this file and rename it to ".env" before running the agent app!
# ============================================================================

# Configuration file for AI Agent Insure - Agent Assist
# All settings can be overridden here. Defaults are defined in agent/config.py

# ============================================================================
# Paths (relative to project root)
# ============================================================================
# PDF_DIR=knowledge-base
# CHROMA_DIR=vector-store/chroma_db
# SQL_DB_PATH=insured-db/insureds.db

# ============================================================================
# RAG Settings
# ============================================================================
# Embedding model for vector embeddings
EMBEDDING_MODEL=nomic-embed-text

# LLM model for generating responses
LLM_MODEL=llama3.2:3b

# Chunk size for document splitting (optimized for better retrieval)
CHUNK_SIZE=1800

# Overlap between chunks (optimized for better retrieval)
CHUNK_OVERLAP=350

# Retrieval score threshold (disabled - ChromaDB doesn't support this natively)
# RETRIEVAL_SCORE_THRESHOLD=

# Retrieval search type: similarity, mmr
RETRIEVAL_SEARCH_TYPE=similarity

# Temperature for LLM (0.0-1.0, higher = more randomness)
TEMPERATURE=0.1

# Number of documents to retrieve (higher K = more context)
RETRIEVAL_K=4

# ============================================================================
# Audio Settings
# ============================================================================
# Whisper model for speech-to-text: tiny, base, small, medium, large
WHISPER_MODEL=base

# ============================================================================
# Gradio Server Settings
# ============================================================================
# Server hostname/IP
SERVER_NAME=127.0.0.1

# Server port
SERVER_PORT=7861

# Create public Gradio link (True/False)
SHARE=False

# Debug mode (True/False)
DEBUG=True

# ============================================================================
# Application Metadata
# ============================================================================
APP_TITLE=AI Agent Insure - Agent Assist
APP_DESCRIPTION=Ask questions about company documents and insured data using text or voice.

# ============================================================================
# SQL Query Settings
# ============================================================================
# Comma-separated prefixes to explicitly trigger SQL queries (optional - intelligent routing detects SQL automatically)
SQL_QUERY_PREFIX=db:,database:

# ============================================================================
# Logging Settings
# ============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log format string
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Log directory (relative to agent folder)
# LOG_DIR=logs

# Log file path (relative to agent folder)
# LOG_FILE=logs/app.log

# Enable file logging (True/False)
LOG_TO_FILE=True

# Enable console logging (True/False)
LOG_TO_CONSOLE=True

# ============================================================================
# Memory Settings
# ============================================================================
# Memory type: buffer, summary
MEMORY_TYPE=summary

# Max tokens for conversation summary (prevents unbounded growth)
MAX_TOKEN_LIMIT=2000
